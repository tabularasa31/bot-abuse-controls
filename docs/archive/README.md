# docs/archive/ — заморожено

Документы здесь описывают **будущую/не-нашу фазу** и не являются текущими
инструкциями. Хранятся для истории, не поддерживаются.

- [`CDN operator-rollout/`](CDN operator-rollout/) — прод-rollout на боевые prod-edge-эджи
  (Puppet/hiera, canary-plan, on-call runbook для CDN operator). Это зона
  CDN operator-админов и будущих фаз (см. [CLAUDE.md](../../CLAUDE.md) «НЕ НАШЕ»).
  Текущая работа идет на демо-стенде; операционные процедуры стенда — в
  [`docs/runbooks/`](../runbooks/), не здесь.
